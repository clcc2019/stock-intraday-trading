---
name: stock-intraday-trading
description: 股票趋势分析与日内做T。100分制评分（基本面50+技术面50），支持选股/做T/日线分析/回测/图表。
---

# 股票趋势分析与日内做T

## 投资哲学

### 两条公设
1. **价格围绕价值波动** — 均线是价值中枢，价格偏离后必然回归
2. **钟摆式过度波动** — 价格因贪婪恐惧过度摆动，偏离越远回归力越大

### 核心原则
- **内功为本招式为辅** — 基本面定方向，技术面把时机
- **顺大势逆小势** — 顺应大趋势，利用小回调进出
- **均线 = 价值中枢** — 均线方向=价值方向，价格与均线距离=钟摆位置

### 评分体系（100分）
- **基本面50分**：盈利(15)+成长(10)+健康(10)+估值(10)+资金(5)
- **技术面50分**：趋势(15)+钟摆(12.5)+强度(10)+量价(7.5)+指标(5)

### 均线层级
| MA250(年线) | MA120(半年) | MA60(季线) | MA20(月线/中枢) | MA5/10(短期情绪) |

## AI执行指令

### 1. 意图识别
- "选股/筛选/推荐" → 选股脚本
- "做T/日内/分时" → T+0脚本
- "回测/历史收益" → 回测脚本
- "图表/K线图" → 图表生成脚本
- 其他 → 日线分析脚本

### 2. 代码识别
常见：贵州茅台(600519)、比亚迪(002594)、恒瑞医药(600276)、宁德时代(300750)

### 3. 执行脚本

**选股**：
```bash
cd .cursor/skills/stock-intraday-trading && python3 scripts/select_stocks.py --index hs300
# 可选: --index zz500/sz50, --no-fundamental(跳过基本面), --sector 板块名
```

**做T分析**：
```bash
cd .cursor/skills/stock-intraday-trading && python3 scripts/analyze_intraday_t0.py 股票代码
```

**日线分析**：
```bash
cd .cursor/skills/stock-intraday-trading && python3 scripts/analyze_stock_simple.py 股票代码
```

**回测**：
```bash
cd .cursor/skills/stock-intraday-trading && python3 scripts/backtest_strategy.py 股票代码
```

**图表**：
```bash
cd .cursor/skills/stock-intraday-trading && python3 scripts/generate_chart_data.py 股票代码 --open
```

### 4. 展示结果
- 直接展示脚本完整输出
- **必须注意数据日期**：报告中有"数据日期"字段，若非当天需向用户说明
- 优先推荐**偏离MA20 < 3%**的股票（回踩到位=最佳买点）
- 偏离MA20 > 5% 的股票标注"追高风险"，建议等回调
- 可用1-2句总结核心建议
- 提醒关注定性因素（管理层、行业格局）

### 5. 数据日期与实时行情
- **baostock K线数据可能延迟1个交易日**（T+1），尤其是周末/节假日
- 脚本已集成 **adata 实时行情**弥补：交易时段价格会自动更新
- 报告中带 `*` 或 `[adata实时]` 标记的是实时价格，无标记的是收盘价
- 周末/非交易时段分析时，**务必告知用户数据为最近交易日收盘价**

### 6. 选股结果解读
- **综合得分** = 技术面×40% + 基本面×40% + 钟摆位置×20%
- 技术面强度已包含 MACD/KDJ 动态加减分：金叉加分、超买扣分
- 钟摆位置权重确保"回踩到位"的股票排名靠前
- 做T评级：⭐⭐⭐ = 趋势强+回踩到位+无超买，最适合操作
- MACD/KDJ超买预警区的股票**不要推荐买入**，即使趋势向上
- 高位提醒区的股票不要推荐买入，除非用户明确要求

### 7. 高位接盘防护（MACD/KDJ）
- 脚本已内置多重超买过滤：
  - **KDJ超买死叉**（J>80 且 K下穿D）→ 重度扣分
  - **J值极端钝化**（J>100）→ 扣分
  - **MACD柱缩短**（价格高位但红柱递减）→ 动能衰竭
  - **MACD死叉**（DIF下穿DEA）→ 重度扣分
  - 多个超买信号叠加（扣分≥4）直接过滤
- 即使趋势向上，若出现超买预警，推荐等 MACD/KDJ 修复后再买入
- 反之，MACD金叉/KDJ金叉会加分，优先推荐

### 8. 磁盘缓存说明
- 历史K线数据自动缓存到磁盘（scripts/.cache/）
- 再次分析同一股票时**只增量更新缺失日期**，性能提升 3-5 倍
- 缓存对用户透明，无需额外操作

### 严格禁止
- 脚本前的冗长说明
- "需要您验证"等推诿话术
- 脚本失败时的理论替代
- 推荐偏离MA20 > 8% 的股票买入（追高）
- 推荐 MACD/KDJ 超买预警区的股票买入

## 错误处理

脚本失败时：显示错误信息，建议检查代码或网络，不输出理论分析。
